spring:
  datasource:
    url: jdbc:mysql://mysql:3306/detection_review
    username: root
    password: 020508
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: redis
      port: 6379
      password: 020508
      lettuce:
        pool:
          max-active: 8
          min-idle: 0
          max-idle: 8
          max-wait: 100

  rabbitmq:
    host: rabbitmq
    port: 5672
    virtual-host: /ebike-helmet-guard
    username: czming
    password: 020508
    connection-timeout: 200ms
    listener:
      simple:
        defaultRequeueRejected: false
        prefetch: 1
        retry:
          enabled: true
          initial-interval: 1000ms
          max-attempts: 2
          multiplier: 1
          stateless: true
    template:
      retry:
        enabled: true
        initial-interval: 1000ms
        multiplier: 1
        max-attempts: 2

